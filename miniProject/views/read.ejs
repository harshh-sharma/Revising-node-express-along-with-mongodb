<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="../stylesheet/style.css">
    <title>Document</title>

</head>
<body>
    <div class="min-h-screen bg-black">
        <a href="/user">All users</a>
        <h1 class="text-white text-3xl my-5">All user</h1>
        <div class="flex gap-3 flex-wrap px-5 w-full justify-center item-center">
        <% users.forEach(function(user){%>
            <div class="w-60 p-2 rounded-md bg-zinc-800 flex flex-col justify-center item-center">
                <div class="w-full h-auto bg-zinc-400 rounded-md mb-3">
                    <img src=<%= user.image %>/>
                </div>
                <div class="text-xl text-zinc-400 mb-3"><%= user.name %></div>
                <div class="text-xl text-zinc-400"><%= user.email %></div>
                <div class="flex gap-2 my-2">
                    <a class="text-zinc-300 text-xl" id="edit" href="/edit/<%= user._id %>">Edit</a>
                    <a href="/delete/<%= user._id %>" class="text-red-500 text-xl">Delete</a>
                </div>
            </div> 
        <%}) %> 
        </div>
    </div>
    <script src="../javascripts/script.js"></script>
</body>
</html>